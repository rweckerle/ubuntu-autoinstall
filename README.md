# Ubuntu autoinstall profiles and ISO creation script

You can use these profiles for autoinstalling Ubuntu via a pxe server or create a flashable ISO Image

## How to use the autoinstall script:

./make-autoinstall.sh <Source ISO> <Profile (arbeitsplatz, vanilla, etc)>

It will create an ISO image in the working directory of the script.
